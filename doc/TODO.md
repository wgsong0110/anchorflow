# 진행 중 작업 (2026-09-23 지시)

## 1. 앙상블 격자 학습·측정
- [x] 원점 어긋난 저해상도 격자 앵상블 구현 (`--ens N`, 변위 평균)
- [ ] `ens4` (16³ x 4) 6000스텝 학습  ← 실행 중
- [ ] 홀드아웃 메트릭 (위치 RMSE / CD / EMD)
- [ ] 추론 속도 측정 (단독 GPU)

## 2. 축 분해 conv 학습·측정
- [x] 순차(`conv_sep`) · 병렬(`conv_par`) 블록 배선
- [ ] `sep32` 6000스텝 학습  ← 실행 중
- [ ] `par32` 6000스텝 학습  ← 실행 중
- [ ] 각각 홀드아웃 메트릭
- [ ] 각각 추론 속도 측정 (단독 GPU)

## 3. 물성 조건부 + 전 조합 학습·측정
- [ ] `--no_mat` 빼고 물성 13채널을 입력에 넣어 학습
- [ ] 지금 완료된 7조합(각 80개)으로 1차 학습
- [ ] 궤적 생성 완료 후 전 조합으로 재학습
- [ ] 조합별 홀드아웃 평가 궤적 생성 (안 쓴 시드)
- [ ] 조합별 메트릭 + 실행 속도

## 기준선 (이미 측정됨)
- conv r32 + 스키닝: 홀드아웃 위치 0.920%(정지 1.515%), CD 비 0.437, EMD 비 0.610
- conv r16 + 스키닝: 홀드아웃 위치 1.082%, CD 비 0.536, EMD 비 0.717
- 속도(mic, 단독): conv 16³ 1.67 ms / 25³ 4.76 ms / 32³ 9.11 ms
